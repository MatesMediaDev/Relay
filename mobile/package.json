{
  "name": "kitsu-mobile",
  "version": "0.3.16",
  "private": true,
  "description": "Kitsu Android — standalone Matrix client (Capacitor)",
  "scripts": {
    "sync": "node sync-www.js && npx cap sync android",
    "open": "npx cap open android",
    "build:apk": "node sync-www.js && npx cap sync android && cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/app": "^7.0.0",
    "@capacitor/core": "^7.0.0",
    "@capacitor/splash-screen": "^7.0.0",
    "@capacitor/status-bar": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0"
  }
}
